import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';

class Button extends PureComponent {
    render() {
        return (
            <div>Ceci est un bouton</div>
        );
    }
}

Button.propTypes = {

};

export default Button;