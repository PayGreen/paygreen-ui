import styled from 'styled-components';
import { layoutStyle } from './base';

const LayoutBase = styled.div`
    ${layoutStyle}
    display: flex;
    overflow-y: hidden;
`;

export { LayoutBase };
