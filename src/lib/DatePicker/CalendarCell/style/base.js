import { css } from 'styled-components';

const selected = css``;

const enabled = css`
    cursor: pointer;
`;

const disabled = css`
    cursor: default;
`;

export { selected, enabled, disabled };
