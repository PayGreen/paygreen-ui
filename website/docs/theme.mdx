---
id: theme
title: 🖌️ Constantes du thème
sidebar_position: 2
---

Les composants de PayGreen UI utilisent tous de nombreuses constantes (couleurs, tailles, media queries&hellip;) définies dans le thème. Certaines de ces constantes sont intéressantes à connaître afin de mieux apréhender le fonctionnement de certains composants.

## Tailles

### Breakpoints des media queries

Ces constantes sont accessibles via `theme.breakpoint['la valeur']`. Pour obtenir les media queries correspondantes, il faut utiliser `theme.screen.min['la valeur']` (pour un écran d'une taille minimale égale à la valeur renseignée) et `theme.screen.max['la valeur']` (pour un écran inférieur à la valeur renseignée).

| Valeur du `breakpoint` | Largeur d'écran |
| ---------------------- | --------------- |
| `sm`                   | 550px           |
| `md`                   | 960px           |
| `lg`                   | 1200px          |
| `xl`                   | 1360px          |

### Spaces

Ces constantes sont accessibles via `theme.space['la valeur']` et sont utilisées pour beaucoup de styles différents (paddings, largeurs&hellip;).

| Valeur du `space` | Taille |
| ----------------- | ------ |
| `none`            | 0      |
| `xs`              | 4px    |
| `sm`              | 8px    |
| `md`              | 16px   |
| `lg`              | 32px   |
| `xl`              | 64px   |

:::caution

Cette documentation est en cours de rédaction et n'est pas encore complète.

:::
